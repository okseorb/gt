import org.openqa.selenium.WebDriver;

public class PageMenuDeliveryAndPayment {
    WebDriver driver;

    public PageMenuDeliveryAndPayment(WebDriver driver) {
        this.driver = driver;
    }
}
